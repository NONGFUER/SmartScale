# 2026-06-26 工作日志

## 水印存证卡片样式按截图重写

- **用户需求**：左下角存证卡片要和截图一模一样（白底蓝边、蓝色盾牌+锁图标、"本图像已通过 密钥存证"、虚线分隔、完整哈希三行显示）。
- **改动文件**：`src/hardware/CameraController.cpp`（`drawWatermarkOverlay` 函数）。
- **关键改动**：
  1. 头文件加 `#include <QPainterPath>`（修复 `QPainterPath shield` incomplete type 编译错误）。
  2. 函数开头 `hashDisplay` 从"前32位+..."改为**完整64位**哈希（配合卡片三行显示 22+22+20）。
  3. 左下角卡片整段重写：
     - 白色圆角背景(250,252,255) + 蓝色边框(70,120,180, 2px) + 圆角12
     - 左侧蓝色盾牌(70,110,170) + 内部白色锁图标（U形弧+矩形锁体+锁孔）
     - 右侧"本图像已通过"/"密钥存证"两行深色粗体标题
     - 虚线分隔线(DashLine, 160,175,195)
     - "存证哈希:"标签 + Consolas等宽字体完整哈希分三行(22+22+20)
- **无 lint 错误**。待用户编译验证视觉效果。

## 水印存证卡片紧凑化调整（对齐目标截图样式）

- **背景**：对比目标样式截图，当前卡片偏宽偏高、元素间距偏大、太"散"。
- **改动文件**：`src/hardware/CameraController.cpp`（左下角卡片块，约 525-604 行）。
- **调整参数（向紧凑靠拢）**：
  - `cardW`: sx(360)→sx(330)，`cardH`: sy(220)→sy(200)
  - 图标 `iy`: sy(12)→sy(10)（上移）
  - `titleLineH`: sy(34)→sy(30)；标题1 sy(14)→sy(12)，标题2 sy(44)→sy(40)
  - `dashY`: sy(84)→sy(72)（压缩标题到虚线间距）
  - 虚线粗细 sx(4)→sx(2)
  - 标签位置 dashY+sy(8)→dashY+sy(4)，高度 sy(26)→sy(22)
  - `hashLineH`: sy(26)→sy(22)，`hashStartY`: dashY+sy(36)→dashY+sy(28)
- **布局核算**：哈希3行底部约到 cardY+sy(166)，cardH=200 留 sy(34) 底边。
- **无 lint 错误**。待编译验证。
