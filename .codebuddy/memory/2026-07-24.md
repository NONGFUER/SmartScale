# 2026-07-24

- SettingsDialog.qml 底部公司信息卡片改版：加入 APP 二维码（resources/img/QRCode.png，已注册到 app.qrc，qrc:/resources/img/QRCode.png）、官网 https://www.sh-xgs.com、联系电话 400-950-8008。布局改为左右结构（左二维码 110×110 + "扫码下载APP"，右公司名/官网/电话/版权），卡片高度 96→184。改资源后需清 build 重新 cmake .. + make -j1。
- 追加：公司信息卡片再加入公众号二维码 resources/img/gzh.jpg（已注册 app.qrc，标签"关注公众号"），布局变为 APP二维码|公众号二维码|文字 三列；二维码 110→100、文字行间距 6→2；用户手动把 APP 标签改为"安卓APP下载二维码"并删除了版权行，保留其改动。
- 修复文字溢出并重排三列：左 APP二维码(88×88,"安卓APP下载")、中间文字列(Layout.fillWidth+每行 fillWidth+居中+elide:ElideRight 防溢出，字号 22/18/18)、右 公众号二维码(88×88,"关注公众号")；RowLayout 改为 anchors.fill+左右 margin 20、spacing 16；卡片高 184→164。

- 应用级远程更新：方案已定稿。触发=应用内手动检查（SettingsDialog「系统更新」卡片）；包内容=v1 仅替换 appSmartScale 单个二进制（QML/图片/字体/AI模型/ORT .so 全编译进或在 applicationDirPath，不进包；data/ 用户库绝不能覆盖）；安装=apply_update.sh 备份回滚（停应用→旧版备份.bak→新版就位→拉起→30s存活验证→失败自动回滚）。已创建 scripts/make_update_package.sh（用法: bash make_update_package.sh <版本号> [build目录]）。用户要求产物仅一个文件：打包 appSmartScale + manifest.json，版本元信息(version/url/notes/force/files[].sha256)全部合并进包内 manifest.json，不再生成外置 version-<ver>.json。首个包：smartscale-2.13.3.24.tar.gz（11MB，实测 gzip 比预估13-17MB更小），包 SHA256=7fc187a33010d20f0e5689721e7b2112d7bc45933464dafb5aea5a0b0091ccb6，输出在 scripts/ 目录。待实施：UpdateService + apply_update.sh + QML 弹窗 + main 接线（设备端"先查版本再下载"的服务器入口清单实施时另定，包内 manifest 同时承载版本指针与文件校验）。

- 生成版本记录文档 docs/版本记录.md：基于完整 git 历史（210 提交，2026-06-13 initial ~ 2026-07-24）按 9 个开发阶段倒序归纳，覆盖设置改版/键盘/4G/语音引擎重构/保存流程/认证/MQTT/品类水印/奠基等；头部说明版本号规则与 version.h 生成链路，附录更新包打包用法。
