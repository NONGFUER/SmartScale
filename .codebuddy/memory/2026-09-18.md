# 2026-09-18

## 排查并修复"手写区采不到点"

### 根因（样式加载路径被系统副本抢占）
- Qt `QQuickVirtualKeyboardSettingsPrivate::stylePath()`：对每个 QML 导入路径拼 `<path>/QtQuick/VirtualKeyboard/Styles/<样式名>/style.qml`，`QFileInfo::exists()` 命中即用，**按导入路径倒序匹配**（越靠后越优先）。
- 系统 `/usr/lib/aarch64-linux-gnu/qt6/qml/QtQuick/VirtualKeyboard/Styles/light/style.qml` 是「改动前仓库版本」的字节级副本（396 行），不含手写三项 → 实际加载的是它 → `KeyboardStyle.traceCanvasDelegate == null` → `TraceInputArea.onPressed` 里 `if (!keyboard.style.traceCanvasDelegate) return` 直接返回 → **一个点都采不到**。
- 反证：app.qrc alias 里那份已含手写三项却仍失效 ⇒ qrc 里的 `:/qt-project.org/imports/...` 路径在该查找逻辑下不可用。

### 修复
1. 立即生效（免重编译）：`sudo cp src/ui/vkbdstyle/light/style.qml /usr/lib/aarch64-linux-gnu/qt6/qml/QtQuick/VirtualKeyboard/Styles/light/style.qml`（已执行，465 行含三项）。
2. 持久修复（不依赖任何系统文件）：
   - `QT_VIRTUALKEYBOARD_STYLE` 由 `light` 改为唯一名 **`smartscale`**（app/main.cpp）。
   - CMake：`file(COPY src/ui/vkbdstyle/light/style.qml DESTINATION <build>/keyboard_styles/QtQuick/VirtualKeyboard/Styles/smartscale)`。
   - main.cpp：`engine.addImportPath(applicationDirPath()+"/keyboard_styles")`。
   - 样式文件头注释同步更新（写明禁止再用 `light` 作为样式名）。
3. OTA：`make_update_package.sh` 资源目录改为循环 `AI` + `keyboard_styles`（进 tar + manifest files[] 逐文件 sha256）；`apply_update.sh` 的 7.5 步同样循环同步这两个目录，失败即在替换二进制前中止（exit 2）。
4. 诊断日志（HandwritingBridge.qml）：启动打印 `VirtualKeyboardSettings.style`、手写可用性、当前输入法 objectName（设为 `PpocrHandwritingInputMethod`）、`patternRecognitionModes`；注入输入法时打印，若 `patternRecognitionModes` 不含 Handwriting 则 warn；新增 `onPatternRecognitionModesChanged` 兜底重新注入。

### 验证
- `main.cpp`/两个 cpp `g++ -fsyntax-only` 通过；qmllint 无新错误；两个脚本 `bash -n` 通过。
- 待用户：重启应用（免重编译路径即可验证）看日志 `[HWR] 提交识别, 笔画数: N`；重编译后日志里的 `[HWR] 键盘样式=` 应变成 `<build>/keyboard_styles/.../smartscale/style.qml`。
- 备用手段：若仍异常，`QML_DISABLE_DISK_CACHE=1` 或删 `~/.cache/appSmartScale/qmlcache`（QML 磁盘缓存）。

## 样式修复后首次实测暴露的两个识别层问题（已修）

实测日志：`[HWR] 提交识别，笔画数: 6` → `[PPOCR] 置信度过低，丢弃识别结果: "鸡" 0.269694` → `[HWR] 未识别出结果`；候选栏只有 `火 ・ 一 ，`。

### 问题 1：置信度阈值把"对的"丢了（已修）
`kMinConfidence` 0.30 → **0.02**（仅过滤纯噪声）。手写体置信度远低于印刷体（正确字常只有 0.2~0.4），阈值必须压到只剩"均匀分布噪声"级别。新增 `kLowConfidenceHint = 0.45f`：低于它只打日志提示，**结果仍然上屏**（预编辑 + 候选纠正）。另加 `kMinInkSize = 6px` 误触过滤（外接框过小的点击不进识别，配合原有的"点数<2 直接丢弃"）。

### 问题 2：候选生成只看"概率峰值那一个时间步"（已修）
旧实现取峰值步 top-5 → 手写时模型常在某些步纠结偏旁，峰值步的正确字可能完全不出现（实测写"鸡"时峰值步 top 全是 火/一/，）。
**改为跨时间步聚合每个类别出现过的最大概率再排序**（`classBest[c] = max_t p[t][c]`，跳过 blank，按概率降序取 top-N，去重），CTC 最优路径结果仍放首位（其重复项先剔除再 prepend）。候选数 5 → **8**（IM 的 `kMaxCandidates = 8`，HwrTestDialog 同步改 8）。
Python 复现新旧算法对比（同一份 logits）：
- 鸡：旧 `鸡 d ，` → 新 `鸡 鸦 鸭 鸥 鸣 鸿 鸤`
- 火：旧 `' . ,` → 新 `火 北 水 次 业 化 比`
- 鱼：旧 `鱼 $ B` → 新 `鱼 。 魚 . 色 」`

### 结论/待办
- 手写链路已完全打通（样式 → 采点 → 识别 → 预编辑/候选 → 落字）。

## 识别率调优（第三轮）

### 消融实验结果（重要：直觉被否掉）
变形字形样本（252 个，旋转±14°/错切/缩放）按留白做消融：
`0.16 → 98.4%`、`0.10 → 97.6%`、`0.07 → 97.2%`、`0.04 → 96.4%`
⇒ **"缩小留白让字形放大"的推理是错的**，留白保持较大值。笔宽无法用印刷字形验证（字形是实心填充，笔宽不起作用），故不能凭推理定值。

### 改动
1. `PreprocessOptions` 结构体化（penWidthRatio / margin / lineMargin / singleCharAspectMax），默认 `{0.08, 0.12, 0.12, 1.5}`；`strokesToImage`/`recognizeStrokes` 加带参重载（**注意**：嵌套结构体 NSDMI 不能直接当同类成员函数的默认实参，必须用重载）。
2. **修正 lineMargin 语义**：原来按"长边"比例留白，字很长时字形被缩得很小；改为按**墨迹高度**留白，单字/长条两种分支缩放到 48px 后字形高度一致（≈38.7px）。
3. **多参数融合 `recognizeStrokesEnsemble`**：三路 `(0.05,0.07) / (0.08,0.12) / (0.12,0.16)` 各识别一次，取**首选置信度最高**的一路作首选、候选取并集（同文本取最高分），首选置顶。IM 改调 ensemble（约 45ms/字）。代理测试：融合 = 最优单路（98.4%），不退化为上限内。
4. 板端**参数扫描**：`Ppocr.recognizeStrokesQmlWithParams(strokes, max, penRatio, margin)`；HwrTestDialog 新增"期望字"输入 + "参数扫描"按钮（5×4 网格，结果进 TextArea 与 `[HWR-SWEEP]` 日志）。**真实手写的最终参数应以该扫描结果为准**。
5. 测试弹窗的识别也走 ensemble（`recognizeStrokesQml` → ensemble），候选数 8。

### 待办
- 用真实手写跑参数扫描（写一个字→填期望字→扫描），据结果决定是否把默认值/融合三路换成实测最优。
- 若仍不满意：换 HWR 专用模型（CASIA-HWDB 系 CRNN）或加入食材词表后处理纠偏。

## 关于"是否要换模型"的结论（给用户的决策框架）

用户 `AI/rec.onnx` **任何情况下都不删**（既是回退也是多字/印刷体场景的主引擎）。三个层次：
- **层次 0（不动模型）**：现状 + 已实现的 TTA 融合 + 食材词表后处理纠偏。食材名场景收益常常最大，成本最低。
- **层次 1（drop-in 换文件，零改代码）**：`PP-OCRv5_mobile_rec` → `PP-OCRv5_server_rec`（同 `[1,3,48,W]` 输入、同 ppocrv5_dict、同 CTC 18385 类）⇒ **只换 rec.onnx 文件即可**，精度提升明显；代价是推理变慢数倍（需实测，估计 15ms → 80~150ms）、体积升到几十 MB；可把 TTA 3 路降到 1~2 路补偿。需要自己用 paddle2onnx 转换或找现成 ONNX。
- **层次 2（真换引擎，改 `PpocrRecognizer` 内部）**：CASIA-HWDB 系**单字**模型（如 HCCR-CNN9Layer：96×96 灰度、**分类头 3926 类**、非 CTC）⇒ 预处理尺寸/通道、解码、词表全变；不支持多字连写/标点/英文（需双引擎调度：单字走 HWR、其余回退 PP-OCRv5）；数据集许可为"研究免费、商用需授权"，现成可商用 ONNX 需自行训练或核实来源。属最后手段。
推荐顺序：0 →（若不够）1 →（仍不够）2；先用板端参数扫描拿到真实数据再决策。

## 书写区放大（用户反馈"有点小"）

两处一起做：
1. **键盘内手写自动放大**：`Main.qml` 里 `InputPanel.scale = hwrBridge.handwritingActive ? 0.80 : 0.62`（加 `Behavior on scale` 动画）。书写区从 ~950×330 → ~1230×576（面积 ≈2.2 倍）；普通输入仍 0.62。
2. **全屏手写**：`Main.qml` 新增全屏层（`Overlay.overlay`，z 100000）：浅色底 Rectangle + Qt 自带 `HandwritingInputPanel{inputPanel; available/active: hwrBridge.fullScreen}` + 自建顶部工具条（提示 / 退格 / 空格 / 回车 / 收起）与自建候选条。入口是键盘左上角按钮行 `[手写/键盘] [全屏手写]`。
   - **注意**：Qt 自带的候选项弹窗 `WordCandidatePopupList` 是**跟随光标位置**（`Qt.inputMethod.cursorRectangle`）定位的，全屏层会盖住它，所以候选条必须自建：`Repeater{ model: InputContext.inputEngine.wordCandidateListModel }`，delegate 用 role `display`，点击调 `model.selectItem(index)`。
   - 工具条按键走 **`InputContext.inputEngine.virtualKeyClick(...)`**（Q_INVOKABLE），而不是 `InputContext.sendKeyClick()` —— 后者直接给焦点控件发原生按键、绕过输入法，预编辑下退格语义会错。
   - `HandwritingInputPanel.available=true` ⇒ `keyboard.fullScreenHandwritingMode=true` ⇒ `handwritingMode=true`（并 bind `keyboard.active=false` 隐藏键盘）；`InputPanel.active` 是 `keyboard.active` 的 alias，所以键盘容器与悬浮按钮会自动隐藏。
3. **新增 `HandwritingInputMethod::finishInput()`（Q_INVOKABLE）**：`commitPending()+clearTraces()`。原因：`InputEngine::setInputMethod()` 只调 `engine.update()`，**不会通知旧输入法**（不调 reset/update），切换输入法时未确认的预编辑字会被丢掉。现在 `HandwritingBridge` 在 `handwritingMode` 变 false 与 `leaveFullScreen()` 时都主动调用它。
4. 样式：`traceCanvasDelegate` 按 `parent.canvasType === "fullscreen"` 用 16px 笔迹（键盘内仍 12*scaleHint）。
5. `main.cpp`：样式环境变量改为「检测文件存在再决定」——存在 `<appdir>/keyboard_styles/.../smartscale/style.qml` 用 `smartscale`，否则 warning 并回退系统 `light`（防"只 make 没 cmake"或旧设备缺目录时退化成内置默认样式→手写区失效）。系统 light 副本已同步保持与仓库一致。

## 全屏手写实时回显（用户要求）

全屏层盖住了输入框 → 在工具条下方加"实时预览"白框（`Main.qml` 的 `fullScreenHwrLayer.previewHtml`），显示：
- **已上屏内容**：`InputContext.surroundingText` 取光标前部分（`cursorPosition` 之前），超过 40 字只留尾部；
- **正在识别的字**：`InputContext.preeditText`，蓝色 + 下划线（`Text.RichText`，注意富文本不支持 `elide`，故用 JS 截尾 + `wrapMode`）；
- 空状态显示灰色提示"在这里手写，写完停顿一下自动上屏"；
- 密码类输入框（hints 含 `Qt.ImhHiddenText`）回显时按 `●` 打码。

关键点：**属性读取必须写在绑定表达式内部**（不要抽成 JS 函数调用），否则 QML 依赖可能建立不上、回显不实时。

输入法侧无需改动（`preeditText` 由 `setPreeditText` 驱动，`surroundingText` 由焦点控件在文本变化时经 `QInputMethod::update(ImSurroundingText)` 刷新）。
- 仍需用户用 SystemInfoDialog→"手写识别测试"实测真实手写首选字正确率；若不满意，可调 `kPenWidthRatio`（笔画粗细，当前 0.055×墨迹边长）、留白比例，或换 HWR 专用模型。
- 可选后续：按 `ImhHiddenText`/`ImhPreferLowercase` 等 hints 强制 Latin/数字模式（当前不按 hints 过滤，避免误挡合法输入，如密码里打中文）。

## 二次实测"没法输出"（模式硬过滤导致，已修）

实测日志：`[PPOCR] 识别: "书" 置信度 0.17 候选数 8` → `[HWR] 预编辑: "3" 候选: QList("3")`；`[PPOCR] 识别: "苦" 0.29 候选数 8` → `[HWR] 候选项均不符合当前输入模式，忽略`。

### 根因
用户当时是**英文 locale + Latin 输入模式**（判定依据：样式 `languageKeyPanel` 显示 `InputContext.locale` 是否以 zh 开头 → 截图显示"英文"；`InputModeKey` 显示的是 `inputModeNameList[InputEngine.inputMode]`，索引 0=ABC → 截图显示"ABC"）。而 `applyInputModeFilter()` 按模式**硬过滤**候选：Latin 模式只留 ASCII → 中文候选全被丢掉（"书"只剩一个游离的 `3` 被当成预编辑；"苦"候选清空直接忽略）。

### 修复
- `applyInputModeFilter()` → **`rankCandidatesByInputMode()`**：模式只做**备选排序偏好**（数字模式数字优先、拉丁模式 ASCII 优先），**绝不丢弃候选**，且**首选（CTC 最优结果）永远不动**；不再做"抽取数字"这种改写（它正是 `书`→`3` 的元凶）。
- `inputModes()`：非 zh locale 也在末尾追加 `ChineseHandwriting`（英文键盘下也能用手写输入中文，且 InputModeKey 可切到"中文"）。
- `publishCandidates()` 日志补 `模式/hints/locale`，便于下次一眼定位。
- 布局侧依据（Qt 源码）：`InputModeKey.text = inputModeNameList[inputEngine.inputMode]`（当前模式）；`ChangeLanguageKey.displayText = keyboard.locale.split("_")[0]`（当前 locale）；本项目样式 `languageKeyPanel` 把它渲染成 "中文"/"英文"。

## 新增功能：按斤显示（重量 + 单价，已落地）

需求：部分客户按"斤"看数，其他客户不需要 → 设置里加开关；**数据接口/DB 仍为 kg、元/kg**。

决策（用户拍板）：①重量与单价都换算；后端 price 字段口径确认为 **元/kg**；②存证照片水印**固定 kg**（`CameraController.cpp:662` 不动）；③称重卡片单位字号 48 → **64**。

实现：
- `AppSettingsService` 新增 `weightUnit`（int，0=kg 默认 / 1=斤，持久化 `AppSettings.conf` 的 `weightUnit`）+ `enum WeightUnit{UnitKg,UnitJin}` + `Q_ENUM`（QML 侧仍用数值比较 `=== 1`）。
- 新增 QML 单例 **`src/ui/WeightUnit.qml`**（`pragma Singleton`，`CMakeLists.txt` 同时改 `QML_FILES` 与 `set_source_files_properties(... QT_QML_SINGLETON_TYPE TRUE)`）：`jin/factor/unit/priceUnit` + `disp(kg)`、`text(kg)`、`price(元/kg)`、`priceToKg(显示价)`。换算：斤=kg×2，元/斤=(元/kg)÷2，金额是不变量。
  - 选纯 QML 单例的原因：绑定会追踪 QML 属性依赖，切开关后所有展示点自动刷新；C++ `Q_INVOKABLE` 内部读属性**不进入依赖追踪**，切开关不刷新。
- 接入点（全部只改展示/输入，未动 DB/上传/paged 解析/阈值判断/水印）：
  - `WorkstationPage.qml`：称重卡片数值 1053 + 单位文案/字号 1063、历史列表项 263、单价标签 757、单价显示 783、弹键盘 797、`onConfirmed` 回写 `priceToKg` 1829。
  - `NumberPadPopup.qml` 162 单位文案；`DuplicateWeightDialog.qml` 130；`WeightRecordSearchDialog.qml` 875；`WeightRecordTableDialog.qml` 表头 472/473 + 单元格 526（重量）/528（单价，经 `WeightUnit.price` 后再 `_fmtPrice`）。
  - `SettingsDialog.qml` 价格输入开关下方新增「按斤显示（1斤=0.5kg）」ToggleSwitch（`onToggled` 写 `AppSettings.weightUnit = checked ? 1 : 0`）。
- 遗留：`SaveConfirmDialog.qml` 仍是**死代码**（无任何实例化），内部硬编码 元/斤 且 `amount = price×weight×2`，与实际保存链路（`SaveSuccessDialog`+`NumberPadPopup`）口径不同，未改动（避免动无关死代码）。
- 未验证（用户自跑）：`make -j1` 编译 + 板端实测切开关后所有页面刷新（read_lints 已通过）。

### 编译报错修正（`Q_ENUM` 必须放在 public 区）
首次编译报 `'AppSettingsService::WeightUnit' is private within this context`（`moc_AppSettingsService.cpp:103/104` 引用 `UnitKg`/`UnitJin`）。
原因：枚举我写在了 `class` 的 **Q_PROPERTY 之后、`public:` 之前**（class 默认 private），moc 生成的 `QMetaEnum` 代码无法访问。
修法：把 `enum WeightUnit { UnitKg = 0, UnitJin = 1 };` + `Q_ENUM(WeightUnit)` 移到 `public:` 之后（现紧跟在 `public:` 下、构造函数之前）。**后续在本项目加带 Q_ENUM 的枚举一律放 public 区**。

### 第二次问题：历史重量/称重数字/单价标签全空白（缺 import SmartScale）
现象：页面上所有走 `WeightUnit.*` 的 `text` 绑定渲染成空白（历史记录重量、称重卡片数字+单位、食材卡"单价（…）"标签），其他控件正常；开关开/关都一样。
根因：**Qt6 隐式导入只覆盖「文件所在目录」下的 QML 文件名，不覆盖整个模块。** `WeightUnit.qml`/`Theme.qml` 在模块根 `src/ui/`，而 `src/ui/pages/WorkstationPage.qml` 在 `pages/` 子目录，两者不共享隐式导入 → `WeightUnit is not defined` → 绑定抛错 → 该 Text 直接空白（`font.family/color` 这类失败则静默回退默认值，所以很难发现）。
权威依据：Qt 官方博客 *Implicit Imports vs. QML Modules in Qt 6*（Ulf Hermann）——"You generally don't have to import the module a QML file belongs to itself" 仅成立在文件与 qmldir 同目录时；并明确警告「同一类型名经命名模块导入是 singleton、经同目录隐式导入却是普通类型」。修法即博客方案 3：**每个文件都显式 import 自己的模块**。
修法：给 `pages/WorkstationPage.qml`、`components/DuplicateWeightDialog.qml`、`components/NumberPadPopup.qml` 各加一行 `import SmartScale`（`SettingsDialog.qml`/`WeightRecordTableDialog.qml`/`WeightRecordSearchDialog.qml` 本来就有）。
审计结果（`grep -rl "WeightUnit\.\|Theme\."` × `grep "^import SmartScale"`）：仍有 6 个文件用 `Theme.*` 但缺导入（`components/` 下 SaveConfirmDialog、AiResultSelectDialog、SaveSuccessDialog、SaveLoadingOverlay、OtaDownloadConfirmDialog，以及 `pages/CategoryCorrectionDialog.qml`）——**既有隐患未修**（只影响字体族/颜色的静默回退，待用户决定是否统一补）。
排查口诀：**文字空白但其他正常 → 先查 `import SmartScale`**。
