# 2026-07-14 工作记录

## WorkstationPage UI 修改
1. **去皮按钮样式重构**：从 `ActionButton` 组件改为自定义绿色圆角 Rectangle（`#0DC218`, radius:24, 50px 白色加粗文字），保留冷却期防抖逻辑
2. **新增底部状态栏 `bottomStatusBar`**：位于 mainCard 最底部，高度 36px，深蓝背景 `#1E40AF`

3. **StatusBar 高度溢出修复**：mainRow 显式设 `height: parent.height`；WiFi 小绿点从溢出锚定改为内部锚定
4. **StatusBar 日期时间排版改为上下两行**：上「2026年4月29日」，下「星期三  01:59:19」
5. **头像白色底色修复**：条件增加 BackendAuth.avatarUrl 判断（source="" 时 Image.status 仍为 Ready 导致底色被跳过）
6. **WorkstationPage RowLayout 边距**：leftMargin/rightMargin=44, bottomMargin=70
7. **新增 BottomStatusBar 组件**：独立底部状态栏（36px 蓝色），含设备型号+版本号+版权信息；已集成 Main.qml mainLayout 底部
   - 左侧：设备型号信息
   - 中间：动态版本号（读取 SystemInfo.appVersion）
   - 右侧：版权声明

15. **AI识别/选择食材按钮文字右移**：从 centerIn 改为垂直居中 + leftMargin:72，避开左侧图标（ai.png/ingr.png 背景图内嵌图标）
16. **StatusBar 大标题显示 custNm**：AuthService 新增 custNm 属性（从 /api/ems/User/by-id 响应解析），StatusBar.qml 大标题绑定 `BackendAuth.custNm || "扬州耀阳"`

17. **StatusBar 图标 Canvas→Image 改造**：4G信号/WiFi/断网/齿轮共4处Canvas全部替换为PNG图片（Signal0~4/Wifi0~4/Setting.png），新增 signalLevel() 映射函数，CMakeLists.txt 注册11个新图标资源，容器统一44x44
18. **StatusBar 布局警告修复**：用户区 MouseArea 原在 RowLayout 内用 `anchors.fill: parent` 触发 `recursive rearrange`，改为用 Item 容器(userArea)包裹 RowLayout+MouseArea，anchors 合法化。经验：RowLayout 直接子项禁写 anchors，须用 Layout.* 或包进非 layout 的 Item
19. **WorkstationPage VideoOutput radius 错误修复**：VideoOutput 继承自 Item 无 radius 属性，原 L462 `radius:24` 导致组件创建失败并连锁 StackView `Component is not ready`。删除该行，圆角由父 Rectangle(radius:20+clip) 裁剪实现。经验：VideoOutput/Image/Item 等无 radius，圆角须靠父 Rectangle clip
20. **LoginDialog hintText anchors 警告修复**：Dialog 直接子项 Text 用 anchors 触发 layout 冲突警告，改为绝对坐标 x:(width-implicitWidth)/2; y:height+8
21. **RecordDetailDialog 图片查看弹窗视觉升级**：
    - 弹窗尺寸放大至 1600x960(max)，`anchors.centerIn: parent` 居中
    - 背景改为深色沉浸式(#1E1E2E, radius:28)，阴影增强(blur:40, opacity:0.35)
    - 图片容器从浅灰(#F8FAFC)改为深黑(#12121A, radius:18)
    - 标题字号28 + letterSpacing:2，关闭按钮圆形(44x44, r:22)
    - 日期时间锚定到大标题右侧(anchors.left:titleText.right)，大标题固定宽度950
22. **StatusBar 日期时间定位调整**：日期时间块从 mainRow 移出，独立锚定到大标题(titleText)右侧(leftMargin:30)；大标题 width 固定 950 避免字数变化影响布局
23. **RecordDetailDialog QML 编译坑（重要）**：
    - QML `color` 属性不支持 CSS 风格 `rgba(...)` 字符串，必须用 `#RRGGBBAA` 或 `Qt.rgba()`（line 63/109/140 曾误用导致 "color expected" 组件加载失败连锁 StackView 报错）
    - `Text` 无顶层 `letterSpacing` 属性，须用 `font.letterSpacing`（曾误用导致 "non-existent property" 报错）
    - 经验：QML 颜色/字体属性与 CSS 写法不同，赋 color 一律用 hex 或 Qt.rgba()
24. **WeightHistoryService buildUploadJson 接口变更**：
    - `val` 字段单位从克(×1000 取整)改为 kg（`QString::number(w,'f',2).toDouble()` 两位小数四舍五入）
    - 新增 `price`(默认0) 和 `amount` 字段
25. **StatusBar 用户区域点击修复**：头像/名字区域原绑定 `settingsRequested()`（弹设备信息），改为新增 `userAreaClicked` 信号，Main.qml 转发到 Main.qml 级别的 logoutConfirmDialog.open()
26. **登录后三个请求改为串行**：将 UserIngredientService.fetchIngredients() 从 QML 移入 AuthService::handleAuthSuccess() 内部统一调度；进一步改为串行链 Product/by-sn → User/by-id → UserIngr/paged，每步成功回调触发下一步，失败时也继续下一环确保全部尝试
