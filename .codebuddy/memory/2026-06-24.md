# 2026-06-24 工作日志

## 修复未登录状态下点击"识别"按钮的拦截缺失

- **文件**：`src/ui/pages/WorkstationPage.qml` 识别按钮 `recognizeMA.onClicked`（约 750 行）
- **缺陷**：原逻辑直接调用 `CameraController.captureVegetable()` 触发识别，未校验 `BackendAuth.currentUser`，导致未登录用户也能发起识别请求。
- **修复**：在 `onClicked` 最前面（重量校验之前）加入登录拦截范式，与同文件"保存"按钮一致：
  ```qml
  if (!BackendAuth.currentUser) {
      console.warn("[WSP] 未登录，拦截识别操作，弹出登录窗口")
      window.toast("请先登录后再识别", "warning", 2000)
      window.showLogin()
      return
  }
  ```
- **登录引导**：弹 warning Toast + 调用 `window.showLogin()` 打开登录弹窗（与保存按钮拦截范式完全一致）。
- **参考范式**：保存按钮 `onClicked`（约 917 行）已有完整的登录拦截写法，可作为后续新增敏感操作的模板。

## 修复识别与保存链路核心 bug + aiDetected 标志位改造

### 1. AI 响应 JSON 解析 bug（CameraController.cpp:805-813）
- **根因**：在线 AI 接口返回 `{"data":{"code":"tudou","name":"土豆"},"success":true}`，`data` 是嵌套对象，原代码 `obj["data"].toString()` 对 QJsonObject 调用返回空串，触发 `isEmpty()` 误判为识别失败 → label 被置为 `unknown`。
- **修复**：先判断 `dataVal.isObject()`，新格式取 `dataObj["code"]` 作为 emsCd，旧格式（data 直接为字符串）兼容保留。同时提取 `message` 字段用于错误诊断日志。

### 2. 保存按钮缺品类/ingrId 校验（WorkstationPage.qml 保存按钮）
- **缺陷**：原 `enabled` 被注释掉（恒为 true），识别失败且未手动选择时直接保存 → `ingrId=""` → `buildUploadJson` 的 `toLongLong("")` 失败 → 上传 `ingrId=0` 的脏数据。
- **修复**：`onClicked` 内增加三道硬校验：①重量 ≤0.01 拦截；②`PState.isValid(currentPrediction)` 品类无效拦截；③`currentIngrId` 为空拦截。每道都有 toast 提示。

### 3. AI 回调识别失败时残留脏 currentIngrId（WorkstationPage.qml onAiRecognitionCompleted）
- **缺陷**：识别返回 unknown 时未清空 `currentIngrId`，上一次有效识别的 ingrId 残留，下次保存会误用。
- **修复**：识别无效时主动清空 `currentIngrId`；反查食材库失败时打 warn 日志并置空。

### 4. aiDetected 标志位改造（贯穿 7 个文件）
- **背景**：用户指出 `json["aiDet"] = !record.categoryName.isEmpty()` 错误——AI 识别和手动选择 categoryName 都有值，导致 aiDet 永远 true。必须按"是否通过 AI 识别接口得出"区分。
- **改动文件**：
  1. `src/data/models/WeightRecord.h`：新增 `bool aiDetected = false`
  2. `src/data/models/WeightRecord.cpp`：`toMap`/`fromMap` 增加 `aiDetected` 字段
  3. `src/data/repositories/WeightRecordRepo.cpp`：`insert`/`update` SQL 增加 `ai_detected` 列 + bindValue；`fromQuery` 读取 `ai_detected`
  4. `src/data/DatabaseManager.cpp`：建表 DDL 增加 `ai_detected INTEGER DEFAULT 0`；新增迁移 v3 `ALTER TABLE weight_records ADD COLUMN ai_detected INTEGER DEFAULT 0`
  5. `src/services/WeightHistoryService.h`：`addRecord` 末尾新增 `bool aiDetected = false` 参数
  6. `src/services/WeightHistoryService.cpp`：`addRecord` 赋值 `model.aiDetected`；`buildUploadJson` 改为 `json["aiDet"] = record.aiDetected`（替换原 `!record.categoryName.isEmpty()`）；ingrId 无效时增加 qWarning
  7. `src/ui/pages/WorkstationPage.qml`：新增 `currentAiDetected`/`pendingSaveAiDetected` 属性；AI 回调成功反查时设 `currentAiDetected=true`，失败时置 false；`onLabelConfirmed`（人工选择/纠错）设 `currentAiDetected=false`；保存按钮暂存 `pendingSaveAiDetected`；`addRecord` 调用传入第 7 参数；`onPhotoSaved` 清理时重置两个属性
- **判定规则**：仅当品类由 `CameraController.onAiRecognitionCompleted` 回调直接得出 → `aiDetected=true`；任何人工选择/纠错路径 → `aiDetected=false`。

### 5. PState::label() 状态提示拆分（src/core/PState.h:32-37）
- **缺陷**：原 `label()` 把 IDLE/NOT_READY/UNKNOWN/NONE 四种语义不同的状态统一返回"点击选择食材"，会误导用户。
- **修复**：按状态分别返回：IDLE→"请识别或选择食材"、NOT_READY→"设备未就绪"、UNKNOWN→"识别失败 请重试"、NONE→"无识别结果"。文本控制在 4-7 字以适配 88px 大字渲染。

## CategoryCorrectionDialog 界面修复 + 删除废弃 FoodSelectPage

- **背景**：FoodSelectPage 分析发现 CategoryCorrectionDialog 存在两处 `font.pixelSize: 32` 笔误（应为 15），以及 getSelectedCn() 冗余 fallback；FoodSelectPage.qml 为未被引用的废弃组件。
- **改动**：
  1. `src/ui/pages/CategoryCorrectionDialog.qml`：
     - L67 `return items[i].cn || items[i].cn` → `return items[i].cn || items[i].en || selectedLabel`（修复冗余 fallback）
     - L226 食品卡片文字 `font.pixelSize: 32` → `15`
     - L265 取消按钮 `font.pixelSize: 32` → `15`（与确认按钮 15px 统一）
  2. 删除 `src/ui/pages/FoodSelectPage.qml`（仅含硬编码测试数据，无业务逻辑，无 QML 引用）
  3. `CMakeLists.txt`：从 qt_add_qml_module 的 QML_FILES 移除 FoodSelectPage.qml 引用

## CategoryCorrectionDialog 遮罩缺失修复（overlay 渲染异常）

- **症状**：dialog 打开后无黑色遮罩，曾经因 `CategoryCorrectionDialog.qml` 缺 `import QtQuick.Effects` 导致 `MultiEffect` 未解析 + `layer.enabled:true` 离屏渲染异常，连带污染整个 WorkstationPage 场景图，主页面只剩背景图。
- **根因**：
  1. import 缺失（已修复，加 `import QtQuick.Effects`）
  2. 当前文件照搬 LoginDialog 的 `modal:false + 外部遮罩`策略，但 WorkstationPage 没有像 Main.qml 那样配套画 Rectangle 遮罩 → 没遮罩
  3. LoginDialog 的遮罩是 Main.qml 里手动画的 `loginOverlay` Rectangle（z:40），**不是** Qt 内置 modal dimmer；LoginDialog 注释明确说"避免 Qt 内部 modal 层遮挡虚拟键盘"
- **修复（方案A）**：`src/ui/pages/CategoryCorrectionDialog.qml` Dialog 内改为 `modal: true` + 显式 `Overlay.modal: Rectangle { color: "#80000000" }`，强制 Qt 用此 Rectangle 替换默认（异常透明）的 dimmer
- **潜在风险**：若此 dialog 内部触发虚拟键盘，modal:true 可能重新遇到 LoginDialog 注释提到的"Qt 内部 modal 层遮挡虚拟键盘"问题，需要观察。如复现需改用方案B（WorkstationPage 内手动遮罩 Rectangle，z:999）

## CategoryCorrectionDialog 搜索框一体化改造

- **背景**：原搜索框的"图标缺失 + 输入框与按钮分离"不符合设计图。目标效果是 🔍 + 输入框 + 搜索按钮三者合一的胶囊形搜索条。
- **文件**：`src/ui/pages/CategoryCorrectionDialog.qml`（原 L109-147）
- **改动**：用一个外层 `Rectangle { radius: 19; color: "#FFFFFF" }` 包裹 `RowLayout`，内部依次为：放大镜 Text（`\u{1F50D}`）→ TextField（`background: null` 用外层统一背景）→ 蓝色渐变搜索按钮（`radius: 16` 胶囊形）。
- **关键点**：TextField 必须设 `background: null`，否则会和外层 Rectangle 叠加渲染出双背景；外层 radius 取高度一半（38/2=19）形成完全圆角胶囊形。

## CategoryCorrectionDialog 完整重构（仿设计稿）

- **背景**：原代码存在多处 RowLayout 子项溢出（搜索按钮、分类标签、底部按钮均超出右边界），用户提供设计稿截图要求完全重写。
- **改动文件**：`src/ui/pages/CategoryCorrectionDialog.qml` — 整体重写
- **设计还原要点**：
  - Dialog 圆角加大到 radius:48，尺寸调整为 width≤1200 / height≤780
  - 顶部栏：返回按钮(40x40, radius10) + 胶囊搜索框(radius21, 内含🔍图标+TextField+蓝色渐变搜索按钮)
  - 分类标签行：改为 Flickable+Row 可横向滚动结构，选中项蓝色(#4361EE)+粗体+下划线指示器
  - 网格区：5列 GridView，卡片圆角12px，使用 HoverHandler/TapHandler 替代 MouseArea（更现代）
  - 底部按钮栏：RowLayout 右对齐(`Qt.AlignRight`)，取消(白底灰边框) + 确认(蓝渐变)，禁用时 opacity 0.45
- **关键修复（消除溢出的根因）**：
  - 所有 RowLayout/ColumnLayout 子项统一使用 `Layout.preferredWidth/Layout.preferredHeight`，杜绝 `width:/height:`
  - 底部按钮栏移除 `Item{ Layout.fillWidth: true }` 弹性占位符，改用 `Layout.alignment: Qt.AlignRight` 整体靠右
  - 分类标签 Repeater delegate 改用 Flickable 包裹实现横向滚动，避免标签过多时溢出
  - 食材卡片交互从 MouseArea 升级为 HoverHandler+TapHandler（Qt Quick 6 新范式）
- **保留的全部业务逻辑**：currentPrediction/currentImagePath/categorySelectMode 属性、labelConfirmed/selectModeToggled 信号、getActiveCategories/getActiveItems/getSelectedCn 函数、onOpened/onAccepted/onRejected 处理器、UserIngredientService/CategoryService/VisionAI 调用链完整保留

