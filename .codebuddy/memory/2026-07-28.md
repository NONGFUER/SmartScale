# 2026-07-28

- 新增更新信息查询接口：UPDATE 域 `https://update.shxgs.cn:5196`（NetworkUtils 加 `UPDATE_BASE_URL` + `Api::UPDATE_INFO_LATEST="/UpdateInfo/latest"` + `createUpdateApiRequest`）。新建 `src/services/UpdateService`（QML 名 `UpdateService`），`checkUpdate()` POST payload 写死 JSON 字符串 `"User_Dzc"`，无需 token；main.cpp 启动后延迟 10s 自动查一次。
- UpdateService 响应打印：`finished` 回调里 `readAll` 后无论成败都用 `qInfo().noquote()` 打印格式化 JSON（`QJsonDocument::toJson(Indented)`，解析失败回退原文），日志落盘 data/smartscale.log 可见。
- StatusBar 4G/WiFi 图标感应区放大：两个 MouseArea 加 `anchors.margins: -16`（44×44 → 76×76 热区），图标视觉与布局不变；图标间距 12，相邻热区边缘重叠约 10px 归后声明者（WiFi/设置）。
- SettingsDialog（设备信息弹窗）新增"版本更新"栏位：SettingRow 显示 `updateVersionText`（检查中.../版本号/获取失败），`onOpened` 调 `UpdateService.checkUpdate()`，`Connections` 监听 `checkFinished` 回写文本（Qt6 函数签名写法 `function onCheckFinished(success: bool, message: string)`）。
- UpdateService 响应解析已定：根 `{data, success, message}`，data 字段 id/appType/verCode/version/pubTime/fileName/url/force/size/differential/verCodeMax/verCodeMin/hash，**数字字段全是字符串形式**（"325"），解析用 `toVariant().toInt/toLongLong()` 兼容；url 是相对路径需拼 UPDATE_BASE_URL；pubTime 秒级时间戳格式化 "yyyy-MM-dd HH:mm:ss"。Q_PROPERTY 平铺：hasInfo/updateId/version/verCode/pubTime/fileName/downloadUrl/force/size/differential/verCodeMax/verCodeMin/hash，统一 infoChanged 信号，checkFinished(true) 时 message=版本号。
- 登录接口（Auth/login）响应结构变更（纯增量，旧解析全兼容）：新增 `refreshTokenExpiration`/`userCode`/`userCust`（数组 [{id,name}]，`devId` 变为字符串 "0"。已改 `AuthService::parseAuthResponse`：**custId 优先取 `userCust[0].id`（17 位雪花 ID，string→toLongLong），custNm 登录即从 `userCust[0].name` 填充并 emit custNmChanged**，无需等 User/by-id；`userCust` 缺失时回退顶层 `custId` 字段（兼容刷新 token 响应无 userCust 的场景）。User/by-id 的 custNm 覆盖逻辑保留。
- User/by-id 传参规则变更：body 不再是 `[FromBody] string userId`（JSON 字符串），改为 JSON 对象 `{"pam1":"<custId>","pam2":"<userId>"}`（雪花 ID 均为字符串）。已改 `AuthService::tryFetchUserInfo`，用 `QString::number(m_custId/m_userId)` 构造。
- **删除启动自动版本检查**：`main.cpp` 删除 `QTimer::singleShot(10000, updateService, &UpdateService::checkUpdate)` — 启动后 10s 无条件自动查询 UpdateInfo/latest，日志显示该请求在登录前就发出。改为仅用户点击齿轮图标打开 SettingsDialog 时按需查询（`SettingsDialog.qml:97` onOpened 保留调用）。
