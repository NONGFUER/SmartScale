# 2026-07-29

- 删除 `src/hardware/WeightSensorWorker.cpp` 第 645 行 `qDebug()` 调试日志 `[Modbus] TX READ_WEIGHT`，减少频繁的 Modbus 轮询 TX 日志输出。
- 修复 `LoginDialog.qml` 错误提示挤压布局问题：错误提示 `Item` 的 `Layout.preferredHeight` 原本是 `errorText.visible ? errorText.implicitHeight : 0`（隐藏时 0、显示时 ~36），错误信息出现时 ColumnLayout 重排挤压 StackLayout、Tab、输入框高度。最初改为固定 36px 占位，但用户反馈无错误时留下空白区域影响美观。最终方案：① `Dialog.height` 动态化 `errorText.opacity > 0 ? 600 : 560`，加 `Behavior on height` 200ms 平滑动画；② 错误提示 Item 保留动态高度（隐藏 0、显示 implicitHeight），加 `Behavior on Layout.preferredHeight` 与对话框同步动画。错误出现时对话框整体向下扩展 40px，错误消失时收缩回 560px，全程无挤压无空白。
