# 2026-06-19 工作日志

## 全屏背景图 + StatusBar 透明化
- **Main.qml**：新增全屏 `Image` 背景图（`z: -1`），`source: "qrc:/SmartScale/img/workstation_bg.png"`，`fillMode: PreserveAspectCrop`
- **StatusBar.qml**：`gradient` 改为 `color: "transparent"`，透出背景图，保留白色文字/图标
- **WorkstationPage.qml**：移除冗余的外层 Item+Image 背景图层，只保留 mainCard 白色圆角主卡片
- CMakeLists.txt 中使用 `RESOURCES` 关键字注册图片（非 IMAGE_FILES，Qt6 不支持）
- **修复括号错误**：移除旧外层 Rectangle 的孤儿闭合 `}`，该括号导致 root Item 提前关闭，flashEffect 在 root 外部引发语法错误

## 动态版本号系统（手动模式）
- 新建 `src/version.h.in` 模板，CMake `configure_file` 生成到 `build/generated/version.h`
- `CMakeLists.txt`：`project(SmartScale VERSION 2.13.2)`，`BUILD_NUMBER` 默认 9，`cmake -DBUILD_NUMBER=N` 手动递增
- `APP_BUILD_DATE` 自动取编译当天日期（`%Y%m%d`）
- `SystemInfoService` 新增 `Q_PROPERTY(QString appVersion)`，格式：`v2.13.2.9_20260619`
- `StatusBar.qml` 第 28 行改为 `"软件版本: " + SystemInfo.appVersion`
