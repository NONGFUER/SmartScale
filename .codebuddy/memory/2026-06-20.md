# 2026-06-20 工作日志

## 食材翻译器去硬编码 — API + 本地 JSON 缓存
- **移除** `FoodTranslator::initDefaultDictionary()` 全部 ~50 条硬编码食材中英文翻译
- **新增缓存机制**：`~/.cache/smartscale/ingredients.json`，启动时 `loadFromCache()` 读取，API 返回后 `saveToCache()` 写入
- **新增 `updateFromApi(QVariantList)`**：从 `UserIngredientService` 的 `m_items` 取 `en`(ingrCd)→`cn`(ingrNm) 映射
- **UserIngredientService::onNetworkReply**：fetch 成功后调用 `FoodTranslator::instance()->updateFromApi(m_items)`
- **LoginPage.qml / LoginDialog.qml**：`onLoginSuccess` 中新增 `UserIngredientService.fetchIngredients()`，首次登录+每次重新登录均触发接口调用并更新缓存
- 次回启动时 `FoodTranslator` 构造直接读缓存 JSON，不再等待接口返回

## 保存按钮移除 BUSY 状态 + 自动模式改为手动 AI
- **保存按钮**：移除 `root.currentPrediction = PState.BUSY`，保存流程不再显示"识别中..."
- **移除 `onStableTriggered`**：称重稳定后不再自动触发拍照+AI
- **新增「识别」按钮**：4 按钮布局（归零/去皮/识别/保存），手动触发 `captureVegetable`→photo→`recognizeLastCapture`→AI。点击时设 BUSY 显示"识别中..."，AI 返回后更新品类名
- 手动流程：放物品→点「识别」→AI 返回品类→点「保存」写入记录
