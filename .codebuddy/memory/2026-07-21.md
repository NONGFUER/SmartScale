# 2026-07-21

- WorkstationPage.qml 智能识别按钮频繁操作锁定规则改为「8 秒内连续点 6 次 → 锁定 20 秒」（原代码 3s 窗口 + >=7 阈值，注释却写 3 次，已对齐）。锁定时长沿用 20s。若用户另有所指（其他按钮/全局锁屏手势）需再调整。

- 快捷登录改造：LoginDialog.qml 的快捷登录页由 ListView 卡片列表改为 ComboBox 下拉选择账号 + 手动输入密码（不记住密码）；doLogin 快捷分支不再调用 loginByHistory，改为 BackendAuth.login(userCode, 输入密码)。AuthService.addLoginHistory 移除 password 参数、停止写入 base64 密码到 login_history.json（注释也已修正为"不含密码"）。切换 Tab / onOpened 时重置 historyCombo.currentIndex 与 quickPwdIn.text。hasRememberedPassword/loginByHistory 变为死代码（暂保留）。

- 快捷登录再次调整（用户新需求）：账号名**不脱敏**（下拉选中项与弹窗项均显示完整 userCode，去掉 `****`+后4位）；快捷登录输入密码登录成功后**记住密码**（新增 AuthService.rememberHistoryPassword(userCode,password) 写入历史 base64 密码），之后打开弹窗若存在已记住密码的历史账号（firstRememberedHistoryIndex()>=0）则直接 loginByHistory **自动登录**。QML 用 pendingQuickUserCode/pendingQuickPassword 在 onLoginSuccess 触发记住；onOpened 末尾自动登录。

- 快捷登录再细化：onOpened 时不再把 historyCombo.currentIndex 重置为 -1，而是默认选中最近登录账号（index 0），免去手动点击下拉选择；自动登录逻辑也改为针对"默认选中的最近账号"（hasRememberedPassword 判断），保证展示与自动登录一致。firstRememberedHistoryIndex 不再被 QML 使用（保留为辅助方法）。

- 设备信息网络控制：目标为 **SettingsDialog.qml**（标题「设备信息」弹窗）。「功能设置」卡片内把「启用4G网络/启用WiFi/网络自动切换」三个开关替换为「网络模式」区：WIFI/4G 状态显示 + 四个 ToggleSwitch 开关行（与「价格输入」同款，互斥单选：仅开启WIFI/仅开启4G/全开优先WIFI/全开优先4G），checked 绑定 netMode===mode，onToggled(checked) 调 setNetMode（关闭当前已选会被绑定弹回）。netMode 与 NetworkManager.networkMode 同步，onOpened 用 refreshNetMode 推导高亮。SystemInfoDialog 误改已撤销。C++ NetworkManagerService 新增 NetworkMode 枚举 + setNetworkMode + networkMode 属性；全开模式用路由 metric 实现优先级（优先=10/非优先=300，重新激活优先连接生效），延迟 8s 应用，metric 持久化。

- **语音播报 TTS 系统改造完成（Piper → sherpa-onnx Matcha）**：VoiceSpeaker 内部完全重写，对外 QML/C++ 接口零变化（speak/stop/warmup/checkSystemReady + 4信号 + 2属性不变）。核心变更：1) `dlopen(RTLD_LOCAL)` 动态加载 sherpa c-api .so，隔离 sherpa 自带 ORT 1.27.0 与项目 ORT 1.24.4 符号冲突；2) 新增 TtsSynthWorker : QObject 移入独立 QThread，持有 sherpa 引擎常驻；3) **惰性线程启动**（构造函数不创建线程，首次 speak() 时才启动 QThread + 初始化引擎，避免影响 app 启动速度）；4) 取消机制：代际检查（主线程 onAudioReady 比对 generation，过期丢弃），不用 sherpa progress callback（避免中断内部推理）；5) 合成结果峰值归一化 float→int16 PCM 后经 aplay stdin 管道播放；6) CMakeLists.txt 新增 dl 库链接 + `3rdparty/sherpa-onnx/c-api.h` 头文件拷贝（官方头文件，避免手写结构体 ABI 不匹配段错误）；7) 路径字符串用 static const QByteArray 防止 qPrintable 悬垂指针。模型路径：`/home/sjwu/matcha-icefall-zh-baker/`，length_scale=0.92, num_threads=1, debug=0。已修复：use-after-free（fnDestroyAudio 后访问 audio->sample_rate）、aplay 泄漏、speak() 中 processEvents() 重入。待用户 `make -j1` 编译验证。
