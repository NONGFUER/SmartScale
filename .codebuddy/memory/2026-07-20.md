# 2026-07-20

## 虚拟键盘：字号加大 + 改悬浮覆盖模式

**用户需求**：键帽字体再大；键盘不要顶升整体界面，而是像手机一样直接浮在上面。

**改动**：
1. `src/ui/vkbdstyle/light/style.qml`：键帽字号统一 64→`80*scaleHint`（视觉 30→37px），字符预览 80→100，长按备选 56→68（候选词/弹窗候选同步 80）。已 `sudo cp` 同步到系统目录 `/usr/lib/aarch64-linux-gnu/qt6/qml/QtQuick/VirtualKeyboard/Styles/light/style.qml`。
2. **悬浮覆盖模式**（废止键盘避让）：
   - `Main.qml` mainLayout：`anchors.bottom` 由 `inputPanel.active ? keyboardContainer.top : parent.bottom` 改为固定 `parent.bottom`（界面不再抬升）。
   - 所有弹窗 y 改纯居中 `(parent.height-height)/2`：Main.qml 内 Login/SystemInfo/WifiList/WifiPassword/Cellular 五个 + 组件弹窗 SaveConfirm/AddIngredient/WeightRecordSearch/CategoryCorrection 四个。
   - 键盘 keyboardContainer z:99 高于所有弹窗（z:50），天然覆盖在上层，无需其他改动。

**注意**：旧记忆里"弹窗 y 避让必须用 keyboardContainer.height"规则已废止，避让公式全部删除。

## 键盘被弹窗遮挡修复（同日追加）

**问题**：悬浮覆盖模式上线后发现键盘被 LoginDialog 等弹窗盖住——Qt 的 Dialog/Popup 渲染在窗口 Overlay 层，普通 item 设 z:99 也压不过。

**修复**：`Main.qml` keyboardContainer 改 `parent: Overlay.overlay` + `z: 99999`（进入 Overlay 层才能真正浮在所有 Popup 之上）。同步更新了 3 处引用 z:99 的注释。read_lints 无错误，待用户实机验证。

## 4G 图标点击弹窗修复

**问题**：StatusBar 里 4G 图标 / WiFi 图标 / 断网占位三个 MouseArea 全部发同一个 `networkRequested()`，Main.qml 无条件打开 WifiListDialog，导致点 4G 图标弹的是 WiFi 列表。

**改动**：
1. `StatusBar.qml`：新增 `signal cellularRequested()`；4G 图标 MouseArea 改发 `root.cellularRequested()`，WiFi/断网占位仍发 `networkRequested()`。
2. `Main.qml`：新增 `Connections { target: statusBar; onCellularRequested: cellularDialog.open() }`。
3. `CellularDialog.qml` 未改——已含运营商 `NetworkManager.cellularOperator` + 信号强度 `NetworkManager.cellularSignal`（进度条+百分比），且全文无 ICCID/IMSI（符合用户明确排除要求）。

**结论**：4G 弹窗数据源是 `NetworkManagerService`（nmcli/mmcli 模式，提供运营商/信号/IP/状态）；ICCID/IMSI 走 `CellularModemService`（AT 串口），只用于 SettingsDialog/SystemInfoDialog，不进 4G 弹窗。

## 4G 弹窗重写 + 控制改 ip link set + 重启记忆

**用户需求**：CellularDialog 仿 WifiPasswordDialog 样式，24px 字体，只留运营商+启用SIM卡开关；控制命令改 `ip link set eth1 down/up`；重启记忆开关状态。

**关键前提（已实测）**：`ip link set` 需 CAP_NET_ADMIN，普通用户 sjwu 无权限（RTNETLINK Operation not permitted）。方案 = sudoers 免密 + `sudo -n ip link set <dev> up/down`。用户已确认自己执行 sudoers 配置：
```bash
echo 'sjwu ALL=(ALL) NOPASSWD: /usr/sbin/ip link set eth1 up, /usr/sbin/ip link set eth1 down' | sudo tee /etc/sudoers.d/smartscale-4g && sudo visudo -cf /etc/sudoers.d/smartscale-4g
```

**改动**：
1. `AppSettingsService` 新增 `cellularEnabled` Q_PROPERTY（QSettings 持久化，**默认 true**），key=`cellularEnabled`，文件 ~/.config/SmartScale/AppSettings.conf。
2. `NetworkManagerService`：新增 `m_sudoPath`/`m_ipPath`（构造时从候选路径解析）；`enableCellularViaDevice()`/`disableCellularViaDevice()` 整体改为 `sudo -n ip link set <dev> up/down`（删掉了原来的 nmcli connection up/device connect/disconnect 三段逻辑）；`onCellularOpFinished` 补 sudo stderr 读取 + "password is required" 专门提示"请配置 sudoers 免密"。
3. `main.cpp`：启动时若 `appSettings->cellularEnabled()==false`，延迟 3 秒（等首轮 refresh 发现硬件）自动调 `disableCellular()` 恢复上次关闭状态。
4. `CellularDialog.qml` 整体重写：modal:false+外部遮罩 z:40；顶部 back2.png+"返回"胶囊(116×44 r22)；标题"4G 移动数据"绝对居中；内容区只有两行——"运营商"(值取 NetworkManager.cellularOperator，按状态显示未启用/搜索中/未检测到模块) + "启用SIM卡"(自定义 Switch 64×36，isSimOn 按 cellularStatus 判定)；底部"关闭"按钮 180×60 r15 浅蓝底；全文字号统一 24px；删掉了原来的状态圆点/信号条/IP/Toast/hintMessage。开关点击先写 `AppSettings.cellularEnabled` 再调 enable/disableCellular，失败用 `window.toast` 提示。

**注意**：ModemManager 模式（enableCellularViaModem/disableCellularViaModem）保留未动，仍走 mmcli；只有以太网接口模式（eth1，用户实际场景）改成了 ip link set。read_lints 全部 0 错误。
