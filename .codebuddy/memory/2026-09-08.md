# 2026-09-08

- 放大主摄取景框与 AI 裁剪区，两处必须同步（保持 UI 框 = AI 实际识别区，避免"假框"导致用户对位偏差）：
  - `src/ui/pages/WorkstationPage.qml` guideBox 宽度系数 0.50 → 0.70
  - `src/hardware/CameraController.cpp` `_processCommon` 主摄像头 `double cropRatio = 0.50` → 0.70
  - 居中锚点不变：x=0.5*width、y=0.53*height（正方形，基于短边 min(w,h)*ratio）。

- 食材接口 `/api/user/UserIngr/paged` 新增 `price` 字段（字符串如 "5.00"，单位元/kg），实现"识别/选菜自动带出价格"：
  - `UserIngredientService.cpp`：fetch 解析 `item["price"]`，saveToCache/loadFromCache 持久化，createIngredient 默认空。
  - `WorkstationPage.qml`：`applyAiSelection`（AI 反查）与 `onLabelConfirmed`（"选择食材"按钮链路）在 `AppSettings.priceInputEnabled` 为 true 时 `currentUnitPrice = Number(item["price"])`；`_executeSave` 改为 `pendingUnitPrice = priceInputEnabled ? currentUnitPrice : 0`。
  - 语义：价格开关开启→自动带出+保存带入；关闭→不显示不填充，保存传 0（金额也 0）。
  - `onCloudSyncSuccess` 回调末尾新增：**无条件** `UserIngredientService.fetchIngredients()` 重拉食材列表（最初加了 `if(AppSettings.priceInputEnabled)`，因该开关默认 false 导致不触发，实测日志确认后改为无条件）。原因：后端在保存（WeightRecord/create-kg）时会回写食材单价，不重拉则下次 AI 识别反查 `findByIngrCd` 用内存旧缓存，带不出/带错价格。与"选择食材"弹窗 onOpened 无条件 fetch 保持一致策略。
  - **修复"调得太早"**（实测日志确认 `cloudSyncSuccess` 是 addRecord 在 DB写入完成就乐观 emit 的，比真正上传完成早约 3s，此时服务器尚未回写价格）：
    - `WeightHistoryService.h` 新增信号 `void cloudRecordUploaded(int localId)`。
    - `WeightHistoryService.cpp` 在 `fromAddRecord=true` 分支 emit 新信号（line 665-667，⑨b 后台上传成功静默分支），与 `cloudSyncSuccess` 完全独立。
    - `WorkstationPage.qml` `onCloudSyncSuccess` 移除 fetchIngredients；新增 `onCloudRecordUploaded` 回调在真正上传完成后调 fetchIngredients。
