# 2026-07-05 工作记录

## 修复 custId/devId 雪花 ID 溢出问题

**问题**：`custId`（雪花 ID `"66885159131025408"`，17位）在 AuthService 解析时用 `toInt()` 导致溢出为 0，称重记录上传时写入错误值。

**修复范围**（4 个文件）：

### AuthService.h
- `Q_PROPERTY(int custId ...)` → `Q_PROPERTY(qint64 custId ...)`
- `Q_PROPERTY(int devId ...)` → `Q_PROPERTY(qint64 devId ...)`
- `int custId() const` → `qint64 custId() const`
- `int devId() const` → `qint64 devId() const`
- `int m_custId = 0` → `qint64 m_custId = 0`
- `int m_devId = 0` → `qint64 m_devId = 0`

### AuthService.cpp
- `custVal.toString().toInt()` → `custVal.toString().toLongLong()`
- `custVal.toInt(0)` → `custVal.toVariant().toLongLong()`
- `devId.toInt(0)` → `devId.toVariant().toLongLong()`

### WeightHistoryService.h
- `revokeRecord(int recordId, int custId, ...)` → `qint64 custId`
- `updateRecordImage(int custId, ...)` → `qint64 custId`

### WeightHistoryService.cpp
- 函数签名同步改为 `qint64 custId`
- `custId = c.isString() ? c.toString().toInt() : c.toInt()` → `.toLongLong()`
- `int uid = ...toInt()` → `qint64 uid = ...toLongLong()`


## WorkstationPage 按钮样式调整

**文件**：`src/ui/pages/WorkstationPage.qml`

**修改内容**：将“选择食材”和“识别”按钮从灰白胶囊文字按钮改为目标图标+文字样式：
- “选择食材”：浅绿色背景（`#DCFCE7`）+ 绿色食材图标（Canvas 绘制的小房子/叶子）+ 绿色文字（`#16A34A`），带悬停/按下状态。
- “识别”：蓝色实心背景（`#60A5FA` → `#3B82F6`）+ 白色扫描/取景图标 + 白色文字；识别中显示白色 loading 指示器并降低透明度。

**实现方式**：使用 `Canvas` 在按钮内直接绘制矢量图标，未引入额外资源文件，保持自包含。

### 按钮位置调整 & 触摸友好放大

**后续修改**：将两个按钮从名称卡片下方移到上方，放入"食材名称"标签同一 `RowLayout`（左侧标签 + `spacer` + 两个按钮靠右）。
同时放大尺寸以适应触摸屏：
- 按钮高度：52→64，圆角：26→32
- 文字 `font.pixelSize`：24→30，图标 Canvas：24×24→32×32
- 标签同步增大：24→28 加粗

### 按钮视觉质感专业化（对齐“保存”按钮）

**后续修改**：将两个按钮的视觉风格从卡通胶囊改为与 `ActionButton` / “保存”按钮一致的商务质感，提升专业度：
- 形状由胶囊（radius 38）改为圆角矩形（radius 12），更稳重商务。
- 背景改为垂直渐变：“识别” `#3B82F6 → #1D4ED8`；“选择食材” `#16A34A → #15803D`。
- 增加顶部白色高光带、底部暗影带、底部散光投影，模拟立体光照与悬浮阴影。
- 边框颜色随悬停变化（蓝 `#1E40AF → #60A5FA`，绿 `#166534 → #4ADE80`）。
- 悬停时按钮轻微上浮（`Translate.y: -2`），按下时回落，提供触觉反馈。
- “选择食材”由浅色 outline 改为深绿填充，图标和文字统一为白色，与“识别”按钮风格统一。
- 动画时长统一为 140ms，过渡更细腻。

## Toast 全局提示组件触摸屏适配

**文件**：`src/ui/components/Toast.qml`

**修改内容**：放大 Toast 气泡的视觉尺寸和内边距，适配触摸屏交互（1920×1080）：
- 气泡高度：56 → 76，圆角：12 → 16
- 最小宽度：200 → 300，内边距：+40 → +64
- 字体大小：18 → 26
- 垂直位置 y：120 → 140，入场动画起点：-80 → -100，退场上移距离：40 → 60
- 注释中的 iconText 像素也同步更新（18 → 22）

## 保存成功语音反馈 + 食材卡片清空

**文件**：`src/ui/pages/WorkstationPage.qml`

### 语音播报
在 `onCloudSyncSuccess` 回调中新增语音播报：`VoiceSpeaker.speak("已保存")`，紧跟 `window.toast("保存成功", "success")` 之后调用。

**术语选择**："已保存"（3字），与现有"识别到XXX"风格统一——简短口语化结果陈述句，便于后续扩展"已上传"等同类播报而不混淆。保存失败场景不播报语音，避免错误场景下反复打扰操作员。

### 食材卡片清空
新增 `clearIngredientCard()` 函数，统一清空食材卡片所有状态：
- `currentPrediction = PState.IDLE`
- `currentImagePath = ""`
- `currentIngrId = ""`
- `currentAiDetected = false`
- `pendingSaveIngrId = ""`
- `pendingSaveAiDetected = false`
- `currentDetailRecord = null`

调用点：
1. `onCloudSyncSuccess` — 云端同步成功后清空
2. `onCloudSyncFailed` — 云端同步失败后清空
3. `onPhotoSaved`（手动保存路径）— 复用 `clearIngredientCard()` 替代原来的逐行赋值
