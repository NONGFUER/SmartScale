# 2026-07-04

## 修复 WeightSensorWorker 串口竞态条件
- **问题**: `poll()` (200ms定时器, Worker线程) 与 `doTare()`/`doCalibrate()`/`doReadSN()` (GUI线程通过QueuedConnection触发) 并发访问同一串口，`m_isBusy` 为普通 `bool` 存在 check-then-set 竞态窗口
- **修复**: 将 `m_isBusy` 从 `bool` 改为 `std::atomic<bool>`；三个命令函数改用 `compare_exchange_strong` 原子抢锁模式；`restartSerial()` 和清除处改用 `store(false, memory_order_release)`
- **文件**: `src/hardware/WeightSensorWorker.h` (加 `<atomic>` 头文件 + 类型声明), `src/hardware/WeightSensorWorker.cpp` (4处函数改动)
