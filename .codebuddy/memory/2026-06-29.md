# 2026-06-29

## CellularDialog 4G QML 渲染问题修复
- **C++ (NetworkManagerService)**: `lastError()` 从 `Q_INVOKABLE` 改为 `Q_PROPERTY` + `NOTIFY lastErrorChanged`，新增 `setLastError()` setter 统一处理 23 处赋值+信号发射。修复 QML 绑定无法追踪 `lastError` 变化的问题。
- **QML (CellularDialog.qml)**: Switch 的 `checked` 属性从 `property bool checked: isSwitchChecked()`（只求值一次）改为直接绑定 `NetworkManager.cellularStatus` 范围比较，确保状态变化时开关 UI 自动同步。

## WorkstationPage.qml anchors 警告
- 第596行 MouseArea 在 RowLayout 内使用 anchors.fill，属于"layout-managed item 上用 anchors"的未定义行为。
- 解决方案：用 Item 包裹 ColumnLayout + MouseArea 作为 RowLayout 唯一子元素（用户尚未确认是否修改）。

## WiFi 断开后状态显示"已连接" bug 修复
- **根因**：`disconnectWifi()` 成功后，10s 轮询 `refreshWifiStatus()` 读到 nmcli 缓存的 connected 状态将 wifiStatus 刷回 Connected
- **修复1**（之前）：新增 `QElapsedTimer m_disconnectTime` 防回退保护窗口（5秒）
- **根因2（真正根因）**：`refreshWifiStatus()` 中状态解析顺序错误——`"disconnected".contains("connected")` 为 true，导致 `state="30 (disconnected)"` 被错误解析为 `Connected(4)`（子串匹配顺序 bug）
- **修复2**（本次）：将 `disconnected/unavailable` 检查移到 `connected` 前面，确保更长的/更具体的词先匹配

## StatusBar WiFi 图标不显示修复
- **根因**：`iconWifi` 的 visible 条件要求 `cellularStatus !== CellConnected && cellularStatus !== CellRoaming`；当 `refreshCellularStatus()` 走网络接口模式匹配到已连接的 ethernet 设备时，`cellularStatus` 被设为 `CellConnected`(4)，导致 WiFi 图标永远不可见
- **修复**：移除 visible 中对 `cellularStatus` 的限制，简化为 `wifiStatus === Connected` 即显示
- **文件**：StatusBar.qml 第 131-133 行

## WiFi 断开连接后页面渲染崩溃（只剩背景+弹窗）修复
- **现象**：点击断开 WiFi 后，mainLayout（StatusBar+StackView）整棵子树消失，仅剩背景图 + WifiListDialog 弹窗
- **根因**：`onStatusPollTimer()` 通过 `QtConcurrent::run` 在工作线程中调用 `refreshWifiStatus()` + `refreshCellularStatus()`，这些方法内部直接修改 Q_PROPERTY 成员变量（m_wifiSsid/m_wifiIpAddress/m_wifiSignal 等）并 emit 信号到 QML。当用户点击断开（主线程执行 disconnect + 属性清空 + emit）与轮询工作线程几乎同时写入同一批属性时，QML 绑定引擎收到矛盾数据 → 放弃渲染 mainLayout 子树
- **修复方案**：
  1. 新增 `runOnMainThread<Func>()` 模板辅助方法：检测当前线程，非主线程时通过 `QMetaObject::invokeMethod(..., Qt::QueuedConnection)` 投递到主线程事件循环串行执行
  2. 所有 setter 方法（setWifiStatus/setCellularStatus/setLastError/updateHasCellularHardware）改为通过 runOnMainThread 执行
  3. refreshWifiStatus()：将解析结果存到局部变量，最后一次性 runOnMainThread 批量更新所有属性 + 统一发射一次 wifiStatusChanged（避免多次 emit 导致 QML 反复重算）
  4. updateCellularStatusFromNmcli/updateCellularStatusFromMmcli：同上模式
  5. onWifiDisconnectFinished/onWifiConnectFinished：批量属性更新也走 runOnMainThread
- **修改文件**：NetworkManagerService.h（+include functional, +runOnMainThread 声明）、NetworkManagerService.cpp（10处修改）
- **设计原则**：nmcli/mmcli 的 I/O 阻塞操作仍在工作线程执行（不阻塞 UI），只有「成员变量写入 + 信号发射」投递回主线程

## WiFi 断开后 mainLayout.visible=false 崩溃（根因修复）
- **现象**：WiFi 断开时 mainLayout 被设为不可见，整个页面白屏
- **根因**：StatusBar.qml 第175行 `onWifiStatusChanged()` 内使用 `parent.visible = ...`，在 `Connections` 作用域中 `parent` 解析到了错误对象，级联将 mainLayout.visible 设为 false
- **修复**：删除 StatusBar.qml 第172-176行的 Connections 块（该代码多余，iconWifi 第131行已有 visible 绑定）
- **附加防护**（Main.qml 已添加）：mainLayout 显式绑定 visible:true + onVisibleChanged 拦截恢复 + 定时巡检

## 虚拟键盘调整
- 默认语言改为英文（`chineseInputMode=false`, `locale="en_GB"`），之前默认中文拼音。
- 中英切换按钮放大：尺寸 60×36→80×50，字号 18→24，圆角 6→8。
- 为 WifiPasswordDialog 密码框和 CategoryCorrectionDialog 搜索框补充 `inputMethodHints: Qt.ImhNoAutoUppercase | Qt.ImhPreferLowercase`，确保所有 TextField 都禁用自动首字母大写（LoginPage、LoginDialog 已有）。
- 键盘整体缩小：InputPanel 设 `scale: 0.82`, `transformOrigin: Item.Bottom`。

## StatusBar WiFi 图标重绘
- **问题**：旧图标用 arc + 偏移圆心 (cy=height*0.62) + 不对称角度绘制，弧线视觉上扭曲不自然，不像真实 WiFi 图标
- **修复**：改用标准 WiFi 图标几何——底部实心圆点作为信号源，3 条弧线从同一圆心向上扇形展开（半径 11/7.2/3.8），角度对称 (-140°~-40°)。同步更新断网图标为相同几何 + 淡色 + 红色斜杠
- **文件**：StatusBar.qml wifiCanvas 和 iconDisconnected 的 Canvas onPaint

## CategoryCorrectionDialog 添加食材入口按钮
- 在品类选择弹窗的食材网格区域（GridView 所在 Item）左下角新增"添加食材"入口按钮（后改为搜索按钮右侧）
- 样式：蓝色渐变按钮 + 圆圈内 `+` 图标 + "添加食材"文字
- 搜索按钮新增 Canvas 放大镜图标

## WeightRecordSearchDialog 称重记录查询弹窗创建
- 新建 `src/ui/components/WeightRecordSearchDialog.qml`：称重记录搜索弹窗
  - 标题栏：返回按钮 + "称重记录查询" + 关闭按钮
  - 搜索条件行：类别下拉（从 historyEntries 自动提取不重复值）、时间下拉（今天/昨天/近7天/近30天）、人员下拉、蓝色搜索按钮
  - 结果区：3列卡片网格（GridView），每张卡片包含：食材图片+记录图像标签、日期时间+人员、食材名称+重量、查看按钮；hover 上浮效果
  - 分页器：共N条 + 上一页/页码/下一页，每页6条（3x2）
  - 数据源：`WeightHistoryService.historyEntries`
  - 对外信号：`viewRecord(var record)` — 点击查看时发射
- 已注册到 CMakeLists.txt QML_FILES

## AddIngredientDialog 创建与接入
- 新建 `src/ui/components/AddIngredientDialog.qml`：添加食材弹窗（标题+关闭、名称输入框+字数统计、分类下拉选择、取消/确认按钮）
- 已注册到 CMakeLists.txt QML_FILES 和 CategoryCorrectionDialog import
- ComboBox 数据绑定修复：model 保持原始 QVariantMap 对象，textRole="cateNm"，confirmed 三参数(name, index, categoryId)
- 崩塌修复：添加缺失的 `import QtQuick.Effects`（MultiEffect 需要）

## 添加食材 API 接入完成（POST /api/user/UserIngr/create）
- **ingrCd 方案**：从拼音改为 UUID 随机编码（取前12位小写），无需外部依赖
- **NetworkUtils.h**:32 — 新增 `USER_INGR_CREATE = "/api/user/UserIngr/create"`
- **UserIngredientService.h** — 新增 `Q_INVOKABLE createIngredient(ingrNm, cateId, cateNm)` + 信号 `createSuccess(ingrId, ingrNm)` / `createFailed(errorMsg)`
- **UserIngredientService.cpp** — `createIngredient()` 实现：生成随机编码→构造 JSON body → POST；`onNetworkReply()` 区分 userIngrCreate/userIngrFetch；创建成功后追加本地 m_items → rebuildCategories → saveToCache → emit itemsChanged + createSuccess
- **AddIngredientDialog.qml** — 确认按钮调用 `UserIngredientService.createIngredient()`，Connections 监听 createSuccess 关闭弹窗
- **CategoryCorrectionDialog.qml** — Connections 监听 createSuccess 自动切换分类+选中新建食材
- **文件**：NetworkUtils.h, UserIngredientService.h/.cpp, AddIngredientDialog.qml, CategoryCorrectionDialog.qml

## WeightRecordSearchDialog 接入 WorkstationPage 入口
- **入口位置**：WorkstationPage.qml 历史记录区域右上角"更多 >"按钮（第108-118行）
  - `onClicked` 从 `console.log(...)` 改为 `searchDialog.open()`，补全 `cursorShape: Qt.PointingHandCursor`
- **弹窗实例**：在 detailDialog 之后新增 `WeightRecordSearchDialog { id: searchDialog }`（第1165-1176行）
  - `viewRecord` 信号桥接到 `root.currentDetailRecord = record → detailDialog.open()`
- **完整链路**："更多>" → searchDialog.open() → 筛选/浏览 → 点击"查看" → 详情弹窗展示大图
- **文件**：WorkstationPage.qml（2处修改，零 lint 错误）
