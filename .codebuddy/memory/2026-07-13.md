# 2026-07-13

## AddIngredientDialog 选中标签不显示（已修复）
- 根因：`ComboBox.displayText` 在 ComboBox 根作用域求值，引用了 `modelData.cateNm`（`modelData` 仅在 delegate 内可用），导致绑定抛错、选中后标签不刷新。
- 修复：displayText 改用 `currentText`（配合已设 `textRole: "cateNm"`）+ 占位文案。两级联动（一级→二级品类）逻辑不变，最终取二级 `cateId` 传给 `createIngredient`。

## CategoryCorrectionDialog 改为两级品类标签（已改）
- 分类标签改为「一级品类 → 二级品类」：一级用 `CategoryService.categoryTree`，二级用其 `children`；点二级后网格按 `cateId` 过滤 `CategoryService.categories` 的食材项。
- 依赖：食材项必须带 `cateId` 字段。原 `parseCategoryResponse` 只解析 `en`/`cn`，已改为保留每个 item 的全部原始字段（cateId/id/img/imgLocal/emsId/cateNm 等），同时规范化 en/cn。
- 品类树加载后默认选中第一个一级+第一个二级，避免网格空白（`onCategoryTreeChanged`）。
- `categorySelectMode`（单级、用 `UserIngredientService.categories`）保留旧逻辑，UI 用 `visible` 切换。
- `onOpened` 增加 `CategoryService.fetchIngrCategories()`；`onAccepted` 改用 `getSelectedItem()`。

## 食材选择弹窗（categorySelectMode）也改为两级品类标签（已改）
- 之前只在 AI 纠错模式（非 categorySelectMode）下做了两级标签；食材选择弹窗（从工作台「选择食材」打开，categorySelectMode=true）仍显示单级标签。
- 修改 CategoryCorrectionDialog.qml：
  - 一/二级标签行对两种模式都可见；旧单级标签行改为 `visible:false`（保留代码便于回退）。
  - `getDisplayItems()` 不再区分模式，统一按 `selectedSubCateId` 过滤 `getAllItems()` 中的食材项。
  - `onOpened` 在 categorySelectMode 下也调用 `CategoryService.fetchIngrCategories()`。
  - `onCategoryTreeChanged` 不再排除 categorySelectMode，两种模式都默认选中第一个一级+第一个二级。
  - 新增 `findCategoryTreePosition(cateId)`，创建食材成功后自动切到新建食材所在的两级分类。
- 顺手删除本文件中残留的两个 `cursorShape: Qt.PointingHandCursor`（触摸屏规则）。

## 一级标签点击后默认选中第一个二级（已改）
- 一级标签 `MouseArea.onClicked` 原来清空 `selectedSubCateId/Name`，导致网格空白。
- 改为：点一级后取 `modelData.children[0]` 自动设为选中二级（无子级才清空），网格立即显示第一个二级分类的食材。

## MEMORY.md 精简合并（维护）
- 因超出大小限制被截断，已重写精简：删除空节「QML 文件注册」；合并「QML 浮层提示规则」+「弹窗遮罩修复范式」为「QML 浮层与弹窗规则」；压缩 nmcli、虚拟键盘、Token 协调器等冗余描述，保留全部硬性规则与关键技术要点。
